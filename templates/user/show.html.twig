{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.Email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.Password }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user.Firstname }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ user.LastName }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ user.Job }}</td>
            </tr>
            <tr>
                <th>Town</th>
                <td>{{ user.Town }}</td>
            </tr>
            <tr>
                <th>Link_Git</th>
                <td>{{ user.LinkGit }}</td>
            </tr>
            <tr>
                <th>Link_LinkedIn</th>
                <td>{{ user.LinkLinkedIn }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user.Description }}</td>
            </tr>
            <tr>
                <th>Skill</th>
                <td>{{ user.Skill ? user.Skill|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Technology</th>
                <td>{{ user.Technology ? user.Technology|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
